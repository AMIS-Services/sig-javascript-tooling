import React from "react";
import * as ReactDOM from "react-dom";

const App = () => {
  return <h1>Hello AMIS!</h1>;
};

export default App;
ReactDOM.render(<App />, document.getElementById("root") as HTMLElement);
